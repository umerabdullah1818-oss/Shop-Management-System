# Used by the Prisma CLI (generate/migrate) when run directly from this
# package. Each app (local-server, cloud-api) supplies its own DATABASE_URL
# at runtime — this file is only for local schema development.
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/shop_local?schema=public"
